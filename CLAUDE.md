# CLAUDE.md — read this first, every session

Project guidance is **shared across all worktrees** and lives at the tree level in
`claude.project.md` (one directory up from this worktree). Edit that file — not this one — so
every worktree and every agent picks up the change. This file only imports it.

@../claude.project.md
